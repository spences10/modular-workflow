name: Main Workflow

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  UBUNTU_VERSION: '22.04'
  NODE_VERSION: '20'
  PNPM_VERSION: '8'

jobs:
  process-pr:
    uses: spences10/modular-workflow/.github/workflows/add-label.yml@main
    with:
      label: 'needs-review'
